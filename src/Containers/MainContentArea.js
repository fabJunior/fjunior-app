// External
import { connect } from "react-redux";
// Ours
import MainContentAreaComponent from "../Components/MainContentAreaComponent";

const mapStateToProps = state => ({});

export default connect(mapStateToProps)(MainContentAreaComponent);
